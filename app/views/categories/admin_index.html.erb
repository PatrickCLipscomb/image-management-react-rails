<div class="container">
  categories admin_index page
  <% if current_user.username == "AlphaAdmin"%>
    <table class="table table-striped table-bordered">
      <thead>
        <tr>
          <th>Category</th>
          <th>Title</th>
          <th>Description</th>
          <th>Image</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @categories.each do |cat| %>
        <% cat.templates.each do |temp| %>
          <tr>
            <th scope="row"><%= cat.name %></th>
            <td><%= link_to temp.title, template_path(temp) %></td>
            <td><%= temp.description %></td>
            <td><%= image_tag "sample_image.jpg", :class => "img-thumb" %></td>
            <td><%= link_to "Delete", template_path(temp), :class => "btn btn-danger", :method => "delete" %></td>
          </tr>
        <% end %>
        <% end %>
      </tbody>
    </table>
  <%end %>
</div>
>>>>>>> 527e54edfc6bd0322e3fb924741404d4b70b4cf2
